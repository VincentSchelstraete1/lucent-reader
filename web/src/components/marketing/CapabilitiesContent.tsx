import { useState } from "react"
import styles from "./marketing.module.css"
const capabilities = [["Explain anything","Turn an unfamiliar concept into a clear explanation grounded in its context.","gradient → stored energy"],["Simplify the page","Adjust dense writing without losing the author’s meaning.","Dense  ⇢  Clear"],["Capture understanding","Keep explanations, your notes, and the source together.","Saved · with context"],["Read your way","Tune level, type, spacing, width, and focus to suit the moment.","Aa  110%  Medium"]]
export function CapabilitiesContent(){const [active,setActive]=useState(0);return <section id="features" className={`${styles.section} ${styles.wideSection}`}><p className={styles.eyebrow}>See what Lucent can do</p><h2 className={styles.sectionHeading}>Small tools. Meaningful shifts.</h2><div className={styles.capabilityGrid}>{capabilities.map((item,index)=><button className={styles.capabilityCard} key={item[0]} onClick={()=>setActive(index)} aria-pressed={active===index}><span className={styles.capabilityPreview}>{active===index?"● ":"○ "}{item[2]}</span><h3>{item[0]}</h3><p>{item[1]}</p></button>)}</div></section>}
