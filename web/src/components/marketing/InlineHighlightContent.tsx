import { useState } from "react"
import styles from "./marketing.module.css"
export function InlineHighlightContent(){const [open,setOpen]=useState(false);return <section className={`${styles.section} ${styles.splitSection}`}><div><p className={styles.eyebrow}>There when you need it</p><h2 className={styles.sectionHeading}>Select something confusing. Lucent appears in place.</h2><p className={styles.sectionBody}>No new tab and no context-switching. Keep your place while the difficult part becomes clear.</p></div><div className={styles.inlineDemo}><p>Memory is strengthened through <button onClick={()=>setOpen(!open)} className={styles.inlineMark}>retrieval practice</button>, the act of bringing information to mind rather than simply reviewing it.</p>{open&&<div className={styles.inlinePopover}><strong>Retrieval practice</strong><p>Testing your memory strengthens it more than rereading does.</p><span>Explain · Note · Save</span></div>}</div></section>}
